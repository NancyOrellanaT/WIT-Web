/* import { assert } from 'chai'
import { shallowMount } from '@vue/test-utils'
import Footer from '@/components/footer/Footer.vue'

describe('Footer', () => {
  let wrapper = shallowMount(Footer)
  
  it('check social media buttons', () => {
    assert.equal(wrapper.find('#buttonFooterFacebook').exists(), true)
    assert.equal(wrapper.find('#buttonFooterTwitter').exists(), true)
    assert.equal(wrapper.find('#buttonFooterInstagram').exists(), true)
    assert.equal(wrapper.find('#buttonFooterLinkedin').exists(), true)
  })
  
}) */